<Window x:Class="Task1.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Task1"
        mc:Ignorable="d"
        Title="Учет сотрудников" Height="450" Width="800">
    <Window.Background>
        <LinearGradientBrush EndPoint="0.5,1" StartPoint="0.5,0">
            <GradientStop Color="White"/>
            <GradientStop Color="White" Offset="1"/>
            <GradientStop Color="Black" Offset="0.5"/>
        </LinearGradientBrush>
    </Window.Background>
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <GroupBox Header="Фильтр по должности" BorderBrush="#FF6A6A6A">
            <StackPanel Orientation="Horizontal">
                <RadioButton Content="Все" IsChecked="True" Checked="FilterChanged"/>
                <RadioButton Content="Программист" Margin="10 0" Checked="FilterChanged"/>
                <RadioButton Content="Архитектор" Margin="10 0" Checked="FilterChanged"/>
                <RadioButton Content="Менеджер" Margin="10 0" Checked="FilterChanged"/>
            </StackPanel>
        </GroupBox>
        <ListView Grid.Row="1" x:Name="EmployeeList" Margin="0 10 0 10" SelectionChanged="EmployeeList_SelectionChanged" Foreground="Black">
            <ListView.View>
                <GridView>
                    <GridViewColumn Header="ФИО" DisplayMemberBinding="{Binding FullName}" Width="300"/>
                    <GridViewColumn Header="Должность" DisplayMemberBinding="{Binding Position}" Width="200"/>
                </GridView>
            </ListView.View>
        </ListView>
        <StackPanel Grid.Row="2" Margin="0 10 0 0">
            <TextBlock Text="ФИО:" Foreground="Black"/>
            <TextBox Name="FullNameBox" Margin="0 5"/>
            <TextBlock Text="Должность:"/>
            <TextBox Name="PositionBox" Margin="0 5"/>
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" Margin="0 10 0 0">
                <Button Content="Добавить" Width="120" Margin="10" Foreground="White" Background="Black"/>
                <Button Content="Редактировать" Width="120" Margin="10" Background="Black" Foreground="White"/>
                <Button Content="Удалить" Width="120" Margin="10" Foreground="Black" Background="White"/>
            </StackPanel>
        </StackPanel>
    </Grid>
</Window>
